/// SF Symbol names constrained to the package's macOS 14 deployment target.
///
/// Keep speculative or newer symbol names out of feature views. These values
/// intentionally favor a simpler, always-rendering glyph over a blank icon.
enum RiffaIcon {
    static let verifiedExchange = "arrow.left.arrow.right"
    static let notEqual = "exclamationmark.circle"
    static let documentRemoved = "doc"
}
